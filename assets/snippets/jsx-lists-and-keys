const users = [
  {
    id: '1',
    firstName: 'John',
    lastName: 'Doe',
  },
  {
    id: '2',
    firstName: 'Jane',
    lastName: 'Deere',
  }
];

const UserList = () => {
  return users.map(user => (
    <p key={user.id}>{user.firstName} {user.lastName}</p>
  ));
};
